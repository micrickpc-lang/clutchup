import { motion,useReducedMotion } from "motion/react";
import { X } from "lucide-react";
import type { Party } from "../../types/api";
import styles from "./PartyFoundScene.module.css";
export function PartyFoundScene({party,close,open}:{party:Party;close:()=>void;open:()=>void}){const reduced=useReducedMotion();const delay=(value:number)=>reduced?0:value;return <motion.div className={styles.scene} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><button className={styles.close} onClick={close} aria-label="Skip"><X/></button><div className={styles.panels}>{party.members.slice(0,3).map((member,index)=><motion.div key={member.user_id} initial={{opacity:0,z:-80,y:10}} animate={{opacity:1,z:0,y:0}} transition={{delay:delay(.1+index*.06),duration:.3}}><span>{member.avatar_url?<img src={member.avatar_url} alt={member.display_name}/>:member.display_name.slice(0,1)}</span><small>{member.display_name}</small></motion.div>)}<motion.i initial={{scaleX:0}} animate={{scaleX:1}} transition={{delay:delay(.33),duration:.22}}/></div><motion.div className={styles.pulse} initial={{scale:.3,opacity:0}} animate={{scale:1.8,opacity:[0,.35,0]}} transition={{delay:delay(.47),duration:.35}}/><motion.p initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} transition={{delay:delay(.56)}}>REQUEST ACCEPTED</motion.p><motion.h1 initial={{opacity:0,filter:"blur(6px)"}} animate={{opacity:1,filter:"blur(0px)"}} transition={{delay:delay(.56),duration:.24}}>PARTY<br/>FOUND</motion.h1><motion.button className="primary-action" initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:delay(.7)}} onClick={open}>OPEN PARTY</motion.button></motion.div>}
